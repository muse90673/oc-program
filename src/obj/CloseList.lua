---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by ThinkPad E475.
--- DateTime: 2019/4/15 0:36
---

PathNode = require("PathNode")

CloseList = {}
CloseList.__index = CloseList

function CloseList:new()
    local o = {}
    setmetatable(o, CloseList)
    o.close = {}
    return o
end

function CloseList:add(pathNode)
    table.insert(self.close, pathNode)
end

function CloseList:remove(pathNode)
    if not self:isEmpty() then
        for k,v in pairs(close) do
            if v==pathNode then
                table.remove(self.close, k)
                break
            end
        end
    end
end

function CloseList:contains(pathNode)
    if not self:isEmpty() then
        for k,v in pairs(self.close) do
            if v.x==pathNode.x and v.y==pathNode.y and v.z==pathNode.z then
                return true
            end
        end
    end
    return false
end

function CloseList:isEmpty()
    if #self.close == 0 then
        return true
    else
        return false
    end
end

return CloseList